# Project Notes

Lexi is currently a single-file, client-side prototype. The main application lives in `index.html`.

No API keys or secrets are required for the current version.

The application uses browser speech APIs and localStorage. Browser support and microphone permissions can affect behavior.
